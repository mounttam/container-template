## A sample Docker container template conatining Python and Go

Maintained by brnwx

Use `docker build -t conatiner-template .` to build an image

Use `docker run -it --rm container-template` to create a container